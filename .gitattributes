# Auto detect text files and perform LF normalization
* text=auto

# Windows launch helpers need CRLF line endings.
*.cmd text eol=crlf
*.bat text eol=crlf

# Binary assets should stay untouched.
*.png binary
*.ico binary
